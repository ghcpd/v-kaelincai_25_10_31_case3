Execution time: (will be measured during execution)
Expected: ~0.3-0.5 seconds (faster due to concurrent execution and no deprecation overhead)
Performance improvements:
  - Concurrent execution with asyncio.gather() provides ~2-3x speedup for async operations
  - No deprecation warnings reducing overhead
  - Modern asyncio event loop management is more efficient
